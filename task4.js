const numA = +prompt("Enter the first number for average:");
const numB = +prompt("Enter the second number for average:");
const numC = +prompt("Enter the third number for average:");

const average = (numA + numB + numC) / 3;

alert("The average is: " + average);