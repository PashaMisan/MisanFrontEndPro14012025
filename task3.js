const value1 = prompt("Enter the first value:");
const value2 = prompt("Enter the second value:");

const areEqual = value1 === value2;

alert("Are the values equal? " + areEqual.toString().toUpperCase());
